#!/usr/bin/env ./node_modules/.bin/babel-node --stage 0

import app from '..';
import http from 'http';

let server = http.createServer(app);
server.listen(process.env.PORT || '4444', process.env.HOST || '0.0.0.0');
